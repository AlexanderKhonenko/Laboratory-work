Написать программу, определяющую попадание точки с определенными координатами в заданную плоскую область. 
Значения координат вводить с клавиатуры. 
Результат работы программы вывести в виде текстового сообщения.

#include "pch.h"
#include <iostream>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	double x, y, r;
	cout << "Введите значение Х: " << endl;
	cin >> x;
	cout << "Введите значение Y: " << endl;
	cin >> y;
	cout << "Введите значение r: " << endl;
	cin >> r;
	if ((y <= 0 && x >= 0 && x*x + y * y <= r * r) || y >= 0 && x <= 0 && (x + r)*(x + r) + (y - r)*(y - r) >= r * r)
		cout << "Точка попадает в область" << endl;
	else cout << "Точка не попадает в область" << endl;
}
