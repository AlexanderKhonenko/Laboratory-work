"""Написать программу, определяющую попадание точки с определенными координатами в заданную плоскую область. 
Значения координат вводить с клавиатуры. 
Результат работы программы вывести в виде текстового сообщения."""

print ("Введите Х")
x = int(input())

print ("Введите У")
y = int(input())

print ("Введите R")
r = int(input())

if r <0:
    print ("Вы ввели некорректный радиус")
else:
    if ((y <= 0 and x >= 0 and x*x + y * y <= r * r) or y >= 0 and x <= 0 and (x + r)*(x + r) + (y - r)*(y - r) >= r * r):
	    print ("Точка попадает в область")
    else:
        print ("Точка не попадает в область") 
